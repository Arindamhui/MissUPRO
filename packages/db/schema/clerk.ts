// Clerk webhook events table — DEPRECATED and no longer used.  
// The table still exists in the DB but is not referenced by any application code.
// Keeping this file as an empty export for backward compatibility with the schema index.
